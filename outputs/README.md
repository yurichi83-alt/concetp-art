# 생성 작업과 최종 전달본

- 날짜별 작업 폴더: 원본 이미지, 모든 시도, 브리프·프롬프트·참조·구조안·검수 기록을 보존합니다.
- `final/`: 요청별 최종 전달 이미지만 모은 폴더입니다. 이미지 파일만 들어갑니다.
- [FINAL_INDEX.md](FINAL_INDEX.md): 이미지·원본 작업·검수 상태를 연결한 목록입니다.
- `final_manifest.json`: 선택 근거·원본 상대경로·복사 이름·SHA-256 및 과거 파일 제외 사유를 기록합니다.

## 전달 기록의 보관 기준

자동 보정 연쇄에서는 실제 최종 전달 대상으로 선택한 이미지만 복사합니다. 파일명의 final, 생성 시각, QA PASS만으로 선택하지 않습니다. delivery/result와 전달 기록이 우선입니다. 여러 장 요청은 각 시안을 포함하며, 사용자 별도 편집 요청은 각 요청의 전달본으로 보존합니다.

검수 보류나 결함이 남은 상태로 전달했던 결과도 상태를 유지해 보관합니다. 이 폴더는 합격작/승인작/새 마스터 목록이 아닙니다. run 원본과 이전 검수는 수정·삭제하지 않습니다.

## 새 run의 품질 비교와 최종 선택 — v2.8 / 실행1.9

각 run의 `requirements.json`이 요청·프롬프트 구절·역할별 참조·실제 관찰·후보 비교를 연결합니다. 기존 기록에 새 양식을 소급 요구하지 않습니다. 실제 입력은 `tool_arguments.json`, 참조는 `references.json`으로 현재값 하나를 기록합니다.

보정 연쇄의 후보를 구조 / 큰 형태 / 외벽 깊이 / 표면 / 빛·재질 / 세계관 / 현재 요청으로 나란히 비교합니다. 같은 시안의 수정 전후 보존은 R03입니다. 최신본이나 구조만 개선된 결과를 자동 최종으로 삼지 않습니다. 한 필수 조건의 개선으로 다른 필수 실패를 상쇄할 수 없습니다.

모든 적용 필수 조건과 QA가 통과해야 `candidate_pass`입니다. 전체 통과가 없으면 미달·불확실 상태와 중단 사유, 후보별 비교 및 선택 이유를 유지합니다. 그런 전달을 보관할 수 있지만 완료/승인으로 부르지 않습니다. 실제 전체·확대 검수는 docs/06_QA.md, 보정/중단 절차는 docs/05_GENERATION_RULES.md를 따릅니다.

`python3 scripts/validate_run.py --run outputs/<run> --stage preflight|review|selection`은 해당 단계의 기록 연결 검사입니다. 정직한 미달 전달의 기록도 PASS할 수 있으며 이는 이미지 PASS가 아닙니다. 수집기는 이미 선택한 파일의 복사만 담당하고 품질 비교나 최선 후보 선택을 하지 않습니다.

## 앞으로 저장하는 방법

1. 생성·보정·전체 검수·후보 비교 후 선택 및 중단/완료 상태를 기록하고 최종 원본을 해당 run에 저장합니다.
2. final_manifest.json의 images에 source(저장소 상대경로), filename(날짜·장면·시안을 구분하는 고유 이름), qa_status, selection_reason, evidence(전달/결과/검수 기록 경로)를 등록합니다. 최초 sha256과 bytes는 생략해도 됩니다.
3. 저장소 루트에서 `python scripts/collect_final_images.py`를 실행합니다. 명시적으로 선택한 원본만 복사하고 해시·목록을 갱신합니다.
4. `python scripts/collect_final_images.py --verify`로 바이트 일치·중복·누락·불필요 파일을 확인합니다.

같은 요청의 선택본을 교체하면 목록과 기존 모음 사본을 함께 정리하되 run 원본·중간본은 유지합니다. 스크립트는 충돌 파일을 임의로 덮어쓰거나 삭제하지 않습니다. 최신 PNG를 자동 추측하여 수집하지 않습니다.

2026-09-11: 전달·검수 기록에서 41장 선별, 참조/중간본68장 제외. 일회성 선별 과정은 scripts/backfill_final_images_20260911.py에 보존했습니다.
