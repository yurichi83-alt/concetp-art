---
name: game-env-art
description: >-
  이 프로젝트에서 교역장 / 밤 / 드럼통 / 한 장 같은 짧은 장소·시간·소재 요청으로 게임 배경 이미지를 생성하거나 수정할 때 사용한다.
  마스터 01~04 v2, 두 출구·단면·중앙 빈 공간·절제된 3D 표현을 유지한다. 코드 앱 개발용이 아니다.
---

# Game Environment Art

이 스킬은 로컬 아트 기준을 실제 이미지 생성 도구에 적용하는 지침이다. 독립 이미지 엔진이나 API 클라이언트가 아니다.

## Locate and read
이 파일에서 위로 찾아 AGENTS.md·project.json이 있는 프로젝트 루트를 확인한다.
루트의 docs/10_CURRENT_REFERENCES.md에서 원본 마스터와 선호 결과의 위계를 확인하고, docs/00_INDEX.md와 상세 마스터 01~04, docs/05_GENERATION_RULES.md 및 docs/06_QA.md를 읽는다.
각 요청 직전 실행 규칙·현재 사용자 요청·refs/manifest.json·명시적 승인 상태를 확인한다.
최신 문서 변경을 반영하되 출력 시안마다 전체 기준을 사용자에게 재설명하지 않는다.

## Act on short commands
1. docs/08_COMMANDS.md에 따라 변경 슬롯을 추출한다. 기존 마스터는 계속 적용한다.
2. 현재 세션의 실제 내장 생성 도구/공식 imagegen 스킬 설명을 확인한다. 존재하지 않는 도구를 호출하지 않는다.
3. M02-01·M01-01·M03-10 등 핵심 원본을 우선 열어 보고 실제 전달한다. 원본 마스터 19장은 계속 활성 기준이다. A-01/A-02는 마스터 기준으로 생성된 선호 결과 예시로 필요에 따라 추가하되 원본을 대체하지 않는다.
4. templates의 형식을 참고해 outputs의 새 run 폴더에 브리프·전달 기록·사전 점검을 작성한다.
5. 준비되면 불필요한 추가 동의 없이 요청한 장수를 실제 생성한다. API·설치·권한 확대가 필요하면 먼저 알린다.
6. 파일 접근이 가능하면 실제 결과를 저장하고 열어 검수한다. 불가능하면 미검수 상태를 기록한다.
7. 결과를 제시하고 규칙 누락이 있으면 숨기지 않는다. 자동 추가 생성이나 자기 승인으로 마스터 변경을 하지 않는다.

## 고정 아트/세계관
Little Devil Inside의 건축 형태·맵핑 텍스처 묘사·라이팅·색감을 참고하되 해당 게임 세계관은 제외한다. 세계관은 Salvage Cyberpunk. 두 방향은 선호 이미지 첨부로 변경하지 않는다.

## Required output intent
단독 쿼터뷰 디오라마 / 베이스 전체 / 노출된 단면 / 뒤쪽 두 면에 출구 하나씩 /
중앙 전투 바닥과 접근로 개방 / 큰 물체 외곽 / 03의 큰 색면과 선택적 디테일 / 04의 수리·개조 첨단기술.

## Host compatibility
상위 시스템과 도구 계약이 우선한다. 생성 도구가 대화의 지시를 직접 사용하는 경우 그 도구의 입력 방식대로 실행한다.
부가 문서나 파일 경로만으로 이미지 참조가 자동 첨부된다고 가정하지 않는다.
생성 후 즉시 종료해야 하는 호스트에서는 후속 저장/QA를 다음 턴으로 미룬다. 보지 못한 결과는 PASS로 기록하지 않는다.
이미지 생성이 불가능하면 막힌 단계를 정확히 보고한다. 앱을 만드는 것으로 요청을 바꾸지 않는다.
